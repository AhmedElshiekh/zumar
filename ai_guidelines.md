# دليل تطوير المشروع (Project Development Guidelines)

## 1. الفلسفة البرمجية
- **اللغة:** Rust (أحدث نسخة مستقرة).
- **النمط:** Clean Code, Idiomatic Rust, Performance-first.
- **الأمان:** استخدام `Result` و `Option` بصرامة. **ممنوع** استخدام `unwrap()` أو `expect()` في كود الإنتاج (Production).
- **اللوجيك:** الحفاظ على سلامة الـ Architecture الموجودة. أي تغيير يجب أن يحترم الـ Ownership & Borrowing rules بشكل كامل.

## 2. بروتوكول العمل (Workflow Protocol)
يجب اتباع الخطوات التالية **حرفياً** قبل كتابة أي كود:
1. **تحليل السياق:** فحص الملفات ذات الصلة والـ `Cargo.toml` للتأكد من توافق المكتبات.
2. **التخطيط:** شرح التعديل المقترح وكيف سيؤثر على المنطق الحالي.
3. **التوقف (Wait):** الانتظار لموافقتي الصريحة قبل كتابة أي سطر كود.
4. **التنفيذ:** كتابة الكود بعد الموافقة، مع توفير تعليقات تشرح الأجزاء المعقدة.
5. **المراجعة الذاتية:** التأكد من عدم وجود تضارب مع الـ Modules الموجودة.

## 3. معايير تقنية
- **التحديثات:** دائماً اقترح استخدام أحدث الـ Crates المستقرة. إذا كان هناك API قديم في المشروع، اقترح تحديثه بدلاً من استخدامه.
- **التنظيم:** الحفاظ على هيكلية الملفات المعتمدة (AGENTS.md, ARCHITECTURE.md).
- **التوثيق:** أي Function جديدة يجب أن تحتوي على doc-comments (`///`).

## 4. التواصل
- كن مختصراً، دقيقاً، ومهنياً.
- إذا كان هناك أكثر من طريقة للتنفيذ، اعرضها عليّ مع مقارنة سريعة (Performance vs Complexity) قبل أن أختار.
